### Blockcheckw

##### При работе над `blockcheckw` РУКОВОДСТВОВАТЬСЯ данными правилами

- Ищи и адаптируй имплементации из `./reference/zapret2/blockcheck2.sh`
- После реализации фичи и подтверждения разработчика, ОБНОВЛЯЙ `./README.md`, чтобы документация была
  всегда актуальной
- НИКОГДА не делай git commit — коммиты делает только разработчик
- Проект собирается под 9 архитектур (x86_64, x86, arm64, arm, mips, mipsel, +mips64, ppc, riscv64)
- НЕ используй типы и API, отсутствующие на 32-битных платформах (например `AtomicU64`). Используй
  платформо-независимые аналоги (`AtomicUsize`, `u32` и т.д.)
- Перед тем как считать работу завершённой, прогони `./ci-local.sh` — он воспроизводит GitHub CI локально (fmt, test,
  clippy)
