#### Blockcheckw
- При работе над `blockcheckw` РУКОВОДСТВОВАТЬСЯ данными правилами
- Ищи и адаптируй имплементации из `./reference/zapret2/blockcheck2.sh`
- СМОТРИ в `./reference/blockcheckw-prototype/blockcheckw/` — это прототип blockcheckw на Kotlin, используй его как референс при реализации на Rust
- СМОТРИ в `/docs/blockcheck2w-rust-architecture` - это основной высокоуровневый документ, допускаются правки
- После реализации фичи и подтверждения пользователя, ОБНОВЛЯЙ `./README.md`, чтобы документация была
  всегда актуальной
- НИКОГДА не делай git commit — коммиты делает только пользователь
